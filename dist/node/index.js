const path=require("path"),{resolve:resolve}=require(path.join(process.cwd(),"./public/node/utils")),fs=require("fs"),os=require("os"),cProcess=require("child_process"),dir=resolve("./tools/dir"),electron=require("electron");let images;try{images=fs.readdirSync(path.join(process.cwd(),"./public/img"))}catch(e){images=[],console.log(e),alert("请执行 npm run getPicture 来爬取背景图片！")}const node={fs:fs,process:process,path:path,os:os,cProcess:cProcess,dir:dir,electron:electron,images:images};window.node=node;